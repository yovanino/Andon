# Common DTOs

Shared API response contracts used by all Andon REST endpoints.

The public API shape is intentionally aligned with the RCA module contract:

- `success`
- `message`
- `data`
- `errors`
- `correlationId`
